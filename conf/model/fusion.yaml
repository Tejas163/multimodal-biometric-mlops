# Architecture defined in BiometricFusionModel — no config needed
# Kept for compatibility with Hydra config system
encoder_hidden_dim: 256
encoder_dropout: 0.0
fusion:
  method: concat
  hidden_dims: [128]
  dropout: 0.5        # matches notebook Dropout(0.5)
num_classes: 45       # overridden at runtime
