# Core ML
torch>=2.1.0

# MLOps / Experiment tracking
mlflow>=2.10.0

# Config management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Data / storage
pyarrow>=14.0.0
numpy>=1.24.0

# Parallel processing
ray>=2.43.0

# Development / testing
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.2.0
mypy>=1.8.0
types-PyYAML

# Image processing (for HOG feature extraction)
scikit-image>=0.21.0
Pillow>=10.0.0
python-dotenv>=1.0.0   # load .env credentials
kaggle>=1.6.0          # Kaggle dataset download CLI
